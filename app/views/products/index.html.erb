<style>
/* Tổng thể */
body {
  font-family: 'Arial', sans-serif;
  line-height: 1.6;
  margin: 0;
  padding: 0;
  background-color: #f9f9f9;
  color: #333;
}

/* Header chính */
h1 {
  text-align: center;
  font-size: 2.5rem;
  margin: 20px 0;
  color: #4CAF50;
}

/* Header phụ */
h2 {
  text-align: center;
  font-size: 1.8rem;
  margin: 10px 0;
  color: #333;
}

/* Liên kết nút */
a {
  text-decoration: none;
  color: #fff;
}

a:hover {
  text-decoration: underline;
}

/* Nút "New product" */
a[href*="new_product"] {
  display: inline-block;
  margin: 20px auto;
  padding: 10px 20px;
  background-color: #4CAF50;
  color: #fff;
  border-radius: 5px;
  font-size: 1rem;
  font-weight: bold;
  text-align: center;
}

a[href*="new_product"]:hover {
  background-color: #45a049;
}

/* Sản phẩm */
#products {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
  padding: 20px;
}

#products div {
  background-color: #fff;
  border: 1px solid #ddd;
  border-radius: 8px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  padding: 15px;
  width: 200px;
  text-align: center;
  transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
}

#products div:hover {
  transform: translateY(-5px);
  box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
}

#products div a {
  display: block;
  font-size: 1.1rem;
  color: #4CAF50;
  font-weight: bold;
  margin-top: 10px;
}

#products div a:hover {
  color: #388E3C;
}
.button-link {
  display: inline-block; /* Chuyển link thành khối inline để áp dụng padding và căn chỉnh */
  background-color: #28a745; /* Màu xanh lá */
  color: white; /* Màu chữ trắng */
  text-decoration: none; /* Bỏ gạch chân */
  font-size: 16px; /* Kích thước chữ */
  font-weight: bold; /* Chữ đậm */
  text-align: center; /* Căn giữa chữ */
  padding: 10px 20px; /* Tạo khoảng đệm để thành nút bấm */
  border-radius: 5px; /* Bo tròn góc nút */
  margin-left: 570px; /* Đẩy nút sang phải 100px */
  transition: background-color 0.3s ease, transform 0.2s ease; /* Hiệu ứng chuyển đổi mượt */
}

.button-link:hover {
  background-color: #218838; /* Màu xanh lá đậm hơn khi hover */
  transform: scale(1.05); /* Tăng kích thước nhẹ khi hover */
}

</style>

<h1><%= t ".title" %></h1>


<h2>Products</h2>

<%= link_to "New product", new_product_path, class: "button-link" %>

<div id="products">
  <% @products.each do |product| %>
    <div>
      <%= link_to product.name, product %>
    
    </div>
      
  <% end %>
</div>
